:root {
  color-scheme: light;
  --bg: #f4efe8;
  --bg-accent: #dfebe6;
  --panel: rgba(255, 255, 255, 0.82);
  --panel-strong: #ffffff;
  --text: #132226;
  --muted: #5c6b70;
  --border: rgba(19, 34, 38, 0.12);
  --accent: #1f6f78;
  --accent-strong: #184f56;
  --danger: #b13f36;
  --success: #1d7b4e;
  --shadow: 0 22px 60px rgba(22, 36, 40, 0.12);
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  font-family: "Manrope", "Segoe UI", sans-serif;
  color: var(--text);
  background:
    radial-gradient(circle at top left, rgba(31, 111, 120, 0.18), transparent 28%),
    radial-gradient(circle at top right, rgba(177, 63, 54, 0.12), transparent 26%),
    linear-gradient(180deg, var(--bg), var(--bg-accent));
}

body::before {
  content: "";
  position: fixed;
  inset: 0;
  background-image: linear-gradient(rgba(19, 34, 38, 0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(19, 34, 38, 0.03) 1px, transparent 1px);
  background-size: 64px 64px;
  mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.35), transparent 80%);
  pointer-events: none;
}

.page-shell {
  width: min(1240px, calc(100% - 32px));
  margin: 0 auto;
  padding: 32px 0 48px;
  position: relative;
  z-index: 1;
}

.hero {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 24px;
  padding: 30px 32px;
  margin-bottom: 20px;
  border: 1px solid var(--border);
  border-radius: 28px;
  background: rgba(255, 255, 255, 0.72);
  box-shadow: var(--shadow);
  backdrop-filter: blur(18px);
}

.hero h1,
.panel h2 {
  margin: 0;
  font-family: "Fraunces", Georgia, serif;
}

.hero h1 {
  font-size: clamp(2.4rem, 5vw, 4.4rem);
  line-height: 0.95;
  letter-spacing: -0.03em;
}

.eyebrow {
  margin: 0 0 10px;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  font-size: 0.76rem;
  color: var(--accent);
  font-weight: 800;
}

.hero-copy {
  max-width: 60ch;
  margin: 14px 0 0;
  color: var(--muted);
  font-size: 1.05rem;
}

.hero-meta {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 10px;
}

.hero-meta span,
.summary-card,
.portfolio-card {
  border: 1px solid var(--border);
  border-radius: 18px;
  background: rgba(255, 255, 255, 0.76);
}

.hero-meta span {
  padding: 10px 14px;
  font-weight: 700;
  color: var(--accent-strong);
}

.flash-stack {
  display: grid;
  gap: 12px;
  margin-bottom: 20px;
}

.flash {
  padding: 14px 18px;
  border-radius: 16px;
  border: 1px solid var(--border);
  background: rgba(255, 255, 255, 0.88);
}

.flash-success {
  border-color: rgba(29, 123, 78, 0.25);
  color: var(--success);
}

.flash-error {
  border-color: rgba(177, 63, 54, 0.25);
  color: var(--danger);
}

.toolbar,
.action-row,
.row-actions {
  display: flex;
  gap: 12px;
  align-items: center;
}

.toolbar {
  justify-content: flex-end;
  margin-bottom: 18px;
}

.grid {
  display: grid;
  gap: 20px;
}

.grid-split {
  grid-template-columns: 0.92fr 1.3fr;
}

.panel {
  padding: 24px;
  border: 1px solid var(--border);
  border-radius: 28px;
  background: var(--panel);
  box-shadow: var(--shadow);
  backdrop-filter: blur(18px);
}

.panel-accent {
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.84), rgba(247, 248, 245, 0.92));
}

.panel-head {
  display: grid;
  gap: 6px;
  margin-bottom: 20px;
}

.panel-head p {
  margin: 0;
  color: var(--muted);
}

.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 0 16px;
  border-radius: 14px;
  border: 1px solid transparent;
  font: inherit;
  font-weight: 800;
  text-decoration: none;
  cursor: pointer;
}

.button-primary {
  color: white;
  background: linear-gradient(135deg, var(--accent), var(--accent-strong));
}

.button-secondary {
  color: var(--accent-strong);
  background: rgba(31, 111, 120, 0.08);
  border-color: rgba(31, 111, 120, 0.18);
}

.button-danger {
  color: white;
  background: linear-gradient(135deg, #d45d54, var(--danger));
}

.portfolio-list {
  display: grid;
  gap: 12px;
}

.portfolio-card {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  padding: 16px 18px;
  color: inherit;
  text-decoration: none;
}

.portfolio-card.is-active {
  border-color: rgba(31, 111, 120, 0.35);
  box-shadow: inset 0 0 0 1px rgba(31, 111, 120, 0.14);
}

.portfolio-card strong,
.summary-card strong,
.stacked-cell strong {
  display: block;
  font-weight: 800;
}

.portfolio-card span,
.summary-card span,
.stacked-cell span,
.muted {
  color: var(--muted);
}

.portfolio-card-meta {
  text-align: right;
  display: grid;
  gap: 4px;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
  margin-bottom: 20px;
}

.summary-card {
  padding: 16px;
}

.summary-card span {
  display: block;
  margin-bottom: 8px;
  font-size: 0.92rem;
}

.positive {
  color: var(--success);
}

.negative {
  color: var(--danger);
}

.position-table-wrap {
  overflow-x: auto;
  margin-top: 18px;
}

.position-table {
  width: 100%;
  border-collapse: collapse;
}

.position-table th,
.position-table td {
  padding: 14px 12px;
  border-bottom: 1px solid rgba(19, 34, 38, 0.08);
  vertical-align: top;
  text-align: left;
}

.position-table th {
  color: var(--muted);
  font-size: 0.82rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.stacked-cell {
  display: grid;
  gap: 4px;
}

.row-actions {
  flex-wrap: wrap;
}

.row-actions a,
.row-actions button {
  border: 0;
  background: transparent;
  color: var(--accent-strong);
  padding: 0;
  font: inherit;
  font-weight: 800;
  cursor: pointer;
}

.empty-state {
  color: var(--muted);
  padding: 10px 0 0;
}

.form-panel {
  max-width: 760px;
}

.form-grid {
  display: grid;
  gap: 16px;
}

.form-grid label {
  display: grid;
  gap: 8px;
}

.form-grid span {
  font-weight: 800;
}

.form-grid input {
  width: 100%;
  min-height: 48px;
  border-radius: 14px;
  border: 1px solid rgba(19, 34, 38, 0.14);
  padding: 0 14px;
  font: inherit;
  background: rgba(255, 255, 255, 0.92);
}

.form-grid select {
  width: 100%;
  min-height: 48px;
  border-radius: 14px;
  border: 1px solid rgba(19, 34, 38, 0.14);
  padding: 0 14px;
  font: inherit;
  background: rgba(255, 255, 255, 0.92);
}

.form-grid input:focus {
  outline: 3px solid rgba(31, 111, 120, 0.15);
  border-color: rgba(31, 111, 120, 0.35);
}

.form-grid select:focus {
  outline: 3px solid rgba(31, 111, 120, 0.15);
  border-color: rgba(31, 111, 120, 0.35);
}

@media (max-width: 960px) {
  .grid-split,
  .summary-grid {
    grid-template-columns: 1fr;
  }

  .hero {
    flex-direction: column;
    align-items: flex-start;
  }
}

@media (max-width: 640px) {
  .page-shell {
    width: min(100% - 20px, 1240px);
    padding-top: 16px;
  }

  .hero,
  .panel {
    padding: 18px;
    border-radius: 22px;
  }

  .action-row,
  .toolbar {
    flex-wrap: wrap;
  }
}